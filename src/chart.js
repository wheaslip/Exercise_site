export function drawChart(canvas,series){const dpr=devicePixelRatio||1,w=canvas.clientWidth,h=canvas.clientHeight;canvas.width=w*dpr;canvas.height=h*dpr;const c=canvas.getContext('2d');c.scale(dpr,dpr);c.clearRect(0,0,w,h);const pad={l:34,r:10,t:15,b:28},iw=w-pad.l-pad.r,ih=h-pad.t-pad.b,max=Math.max(5,...series.map(x=>x.value));c.strokeStyle='#29313d';c.fillStyle='#8591a3';c.font='11px system-ui';c.textAlign='right';for(let i=0;i<=4;i++){const y=pad.t+ih*i/4;c.beginPath();c.moveTo(pad.l,y);c.lineTo(w-pad.r,y);c.stroke();c.fillText(Math.round(max*(4-i)/4),pad.l-7,y+4)}const gap=iw/series.length,bw=Math.max(1,gap*.68);series.forEach((x,i)=>{const bh=x.value/max*ih;c.fillStyle='#f15b3a';c.fillRect(pad.l+i*gap+(gap-bw)/2,pad.t+ih-bh,bw,bh)});c.fillStyle='#8591a3';c.textAlign='center';const labels=Math.min(7,series.length);for(let i=0;i<labels;i++){const n=Math.round(i*(series.length-1)/(labels-1||1));c.fillText(series[n].date.slice(5),pad.l+(n+.5)*gap,h-7)}}
